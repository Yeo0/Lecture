<<<<<<< HEAD
barplot(table
plot(factor
-> plot(state.region)
   barplot(table(state.region)) + horiz=T/F

boxplot ÀÌ horizontal

pir(x,labels
class(state.region)
tab=table(state.region)
barplot(tab)
names(tab)

pie(tab,labels=names(tab))
percent=(tab/sum(tab))*100
tab/sum(tab)
sum(tab)

pie(tab,labels=paste(names(tab),"(",percent,"%)","\n",sep=""))
library(plotrix)
pie3D(tab,labels=paste(names(tab),"(",percent,"%)","\n",sep=""),cex=0.8)

fan.plot(tab,labels=paste(names(tab),"(",percent,"%)","\n",sep=""))
title(main="FP")


dotchart(as.numeric(tab),labels=names(tab))
stem(as.numeric(tab),scale=0.5)

library(UsingR)
str(alltime.movies)
attach(alltime.movies)
boxplot(Gross,range=2, horizontal=T)
str(boxplot)
str(boxplot(Gross))
BG=boxplot(Gross)
BG$out
hist(Gross)
hist(Gross,breaks=c(100,300,length=10)

myid=which(table(waiting)==max(table(waiting)))
table(waiting)[myid]

str(cfb)
attach(cfb)
hist(INCOME)
logincome=log(INCOME)
range(logincome)
hist(logincome)
logincome1=log(INCOME+1)
range(logincome1)
hist(logincome1,breaks=30,freq=F)

mean(INCOME<=mean(INCOME))







=======
barplot(table
plot(factor
-> plot(state.region)
   barplot(table(state.region)) + horiz=T/F

boxplot ÀÌ horizontal

pir(x,labels
class(state.region)
tab=table(state.region)
barplot(tab)
names(tab)

pie(tab,labels=names(tab))
percent=(tab/sum(tab))*100
tab/sum(tab)
sum(tab)

pie(tab,labels=paste(names(tab),"(",percent,"%)","\n",sep=""))
library(plotrix)
pie3D(tab,labels=paste(names(tab),"(",percent,"%)","\n",sep=""),cex=0.8)

fan.plot(tab,labels=paste(names(tab),"(",percent,"%)","\n",sep=""))
title(main="FP")


dotchart(as.numeric(tab),labels=names(tab))
stem(as.numeric(tab),scale=0.5)

library(UsingR)
str(alltime.movies)
attach(alltime.movies)
boxplot(Gross,range=2, horizontal=T)
str(boxplot)
str(boxplot(Gross))
BG=boxplot(Gross)
BG$out
hist(Gross)
hist(Gross,breaks=c(100,300,length=10)

myid=which(table(waiting)==max(table(waiting)))
table(waiting)[myid]

str(cfb)
attach(cfb)
hist(INCOME)
logincome=log(INCOME)
range(logincome)
hist(logincome)
logincome1=log(INCOME+1)
range(logincome1)
hist(logincome1,breaks=30,freq=F)

mean(INCOME<=mean(INCOME))







>>>>>>> 704346157696d5c879444e5bc135c15fa187d2e1
